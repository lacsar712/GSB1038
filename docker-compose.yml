version: '3.8'

services:
  # MySQL 数据库
  db:
    image: mysql:8.0
    container_name: family-contacts-db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: contacts_db
      MYSQL_ROOT_HOST: '%'
    command: --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost", "-uroot", "-proot"]
      interval: 3s
      timeout: 5s
      retries: 20
      start_period: 20s

  # FastAPI 后端：必须等待 db 健康后再启动
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: family-contacts-backend
    restart: always
    ports:
      - "8038:8000"
    environment:
      DATABASE_URL: mysql+pymysql://root:root@db:3306/contacts_db?charset=utf8mb4
    depends_on:
      db:
        condition: service_healthy
    # 仅挂载源码目录，避免覆盖镜像内的 /app/.venv 依赖环境
    volumes:
      - ./backend/app:/app/app
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  # React 前端：必须等待 backend 健康后再启动
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: family-contacts-frontend
    restart: always
    ports:
      - "3038:80"
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:80"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  mysql_data:
    driver: local

networks:
  default:
    name: family-contacts-network
